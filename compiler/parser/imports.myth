use package::parser::{tree_node, tree::TreeErr}

# Todo
# Only the main file can import other source code files.
# This will tokenize any imports and parse them.
fn parse_import(root: [Node]) TreeErr![Node] {
    
}